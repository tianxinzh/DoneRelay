interface:
  display_name: "DoneRelay"
  short_description: "Remote task updates and human confirmations"
  default_prompt: "Use $donerelay to notify me or request a specific answer through my configured bridge."
policy:
  allow_implicit_invocation: false
