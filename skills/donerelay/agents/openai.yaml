interface:
  display_name: "DoneRelay"
  short_description: "Slack self updates and Telegram remote decisions"
  default_prompt: "Use $donerelay for task notifications or a bounded human answer using my chosen channel; reuse my existing Slack connection for self notifications. Never bypass native permissions."
policy:
  allow_implicit_invocation: false
