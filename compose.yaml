# Developer tests only: no separate bridge deployment, credentials, or published ports.
services:
  tests:
    build: .
    init: true
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
